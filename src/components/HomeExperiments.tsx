// HomeExperiments.tsx - ARCHIVE OF ALL HOME PAGE LAYOUTS
'use client';
import React, { useState, useEffect } from 'react';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from './ui/card';
import { Button } from './ui/button';
import { Input } from './ui/input';
import { Badge } from './ui/badge';
import { MagnifyingGlassIcon, MapPinIcon, StarIcon, BoltIcon, UserGroupIcon, ChartBarIcon, BuildingOfficeIcon, RocketLaunchIcon, GlobeAltIcon, UserIcon } from '@heroicons/react/24/outline';
import ThemeToggle from './ThemeToggle';

// --- Inline animation/utility components ---
// (Paste Starfield, DynamicWaves, RiverParticles, rand here)

// ...
// (Paste all layout components: RiverHeroLayout, GeometricModernLayout, etc.)
// ...
// (Paste HomeExperiments function here, including theme dropdown if present)
// ...

export default HomeExperiments; 